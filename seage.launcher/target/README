To run the project from the command line, go to the target folder and
type the following:

java -jar "bin/seage.launcher.jar" 


example:

java -jar "bin/seage.launcher.jar" -run -config config/config-tsp-sa.xml
